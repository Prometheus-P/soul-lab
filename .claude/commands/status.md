---
description: Show project status overview
---

Provide a comprehensive project status:

1. Git status (`git status`)
2. Open issues count (`gh issue list --state open | wc -l`)
3. Recent commits (`git log --oneline -5`)
4. Branch info (`git branch -v`)
5. Any uncommitted changes summary
