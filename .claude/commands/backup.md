---
description: Create data backup (server)
---

1. Run `cd server && pnpm backup` to create a local backup
2. Report backup details:
   - Backup filename
   - Size
   - Files included
3. List existing backups with `pnpm backup --list`
