---
description: Run all tests and fix failures
---

Run the following test steps:

1. Run `pnpm test:all` to execute all tests (frontend + backend)
2. If any tests fail:
   - Analyze the failure reason
   - Fix the failing tests or code
   - Re-run to verify
3. Report test coverage summary if available
