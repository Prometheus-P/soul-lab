---
description: Fix lint errors and type issues
---

Run the following fix steps:

1. Run `pnpm lint` to check for lint errors
2. Run `pnpm build:web` to check for TypeScript errors
3. If errors found:
   - Fix each error systematically
   - Re-run to verify fixes
4. Report summary of fixed issues
